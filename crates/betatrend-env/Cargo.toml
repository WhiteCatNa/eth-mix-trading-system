[package]
name = "betatrend-env"
version.workspace = true
edition.workspace = true
description = "ETH timing env: causal features, overlay/backtest exec, stepwise reward"

[lib]
name = "betatrend_env"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
